<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Xtream API erişimi için. Bazı sağlayıcılar düz HTTP veriyor; cleartext
         configurable şekilde açık (debug için yeterli, prod'ta NSC ile daraltılır). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Downloads: background HTTP transfer + the user-visible foreground
         service that keeps the OS from killing it. Notifications + wake lock
         are mandatory for `setForegroundAsync` on API 31+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".AnotherIptvPlayerApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AnotherIPTVPlayer">

        <!-- `supportsPictureInPicture` + the orientation/screenSize/etc. config
             changes are required for the system to keep our Activity instance
             alive when entering PiP. Without them the OS recreates the
             Activity and the mpv handle gets destroyed mid-transition. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboard|keyboardHidden|navigation|uiMode"
            android:theme="@style/Theme.AnotherIPTVPlayer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- WorkManager's foreground service. `DownloadWorker` calls
             `setForegroundAsync` with `FOREGROUND_SERVICE_TYPE_DATA_SYNC` on
             API 34+ (see DownloadWorker.kt:170), and the OS rejects the
             foreground promotion unless the manifest declares the matching
             `foregroundServiceType`. We merge the attribute into the
             service entry that the work-runtime AAR already provides,
             instead of replacing it (which would lose the library's
             intent-filter / flags). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>